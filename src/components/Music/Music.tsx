import React, {FC} from 'react';
import s from './Music.module.css'

type MusicPType = {}

export const Music: FC<MusicPType> = (props) => {
    const {} = props;
    return (
        <div>
            That's music
        </div>
    );
};
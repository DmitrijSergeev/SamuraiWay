import React, {FC} from 'react';
import s from './News.module.css'

type NewsPType = {}

export const News: FC<NewsPType> = (props) => {
    const {} = props;
    return (
        <div>
            That's news
        </div>
    );
};
